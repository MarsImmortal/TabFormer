name: my_env
channels:
  - anaconda
  - conda-forge
  - pytorch
  - huggingface
dependencies:
  - python=3.8
  - pytorch>=2.0.0
  - torchvision
  - cpuonly  # Ensure CPU-only for osx-arm64
  - numpy
  - pandas
  - scikit-learn
  - transformers
